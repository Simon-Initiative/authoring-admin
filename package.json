{
  "name": "achilles",
  "version": "1.0.0",
  "description": "admin application and elm testbed",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "build": "elm make --output=elm.js src/Main.elm",
    "serve": "elm-live src/Main.elm --port=7000 --open --pushstate --host=dev.local --debug -- --output=elm.js"
  },
  "author": "",
  "license": "ISC"
}
